{
  "name": "raven",
  "version": "1.0.0",
  "description": "Cloudflare Worker for handling incoming emails",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250614.0",
    "wrangler": "^3.0.0"
  }
}
